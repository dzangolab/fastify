/* istanbul ignore file */
import { describe, expect, it } from "vitest";

describe("Firebase Service", () => {
  // TODO: Replace placeholder test
  it("Should create an firebase admin instance", () => {
    expect(false).toBe(false);
  });
});
